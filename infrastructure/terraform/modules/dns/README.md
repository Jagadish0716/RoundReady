# DNS/ACM module

Planned interface: Route 53 hosted-zone records and ACM certificates with DNS validation. TLS
terminates at the public ALB; domain names and hosted-zone ownership are environment inputs.
