# RDS PostgreSQL module

Planned interface: private subnet group, encryption, backups, maintenance window, monitoring,
security groups, and one logical database/owner boundary per persistent RoundReady service.
RDS is authoritative persistence and is never deployed inside EKS.
