# ECR module

Planned interface: one repository per deployable service, immutable tags, scan-on-push,
lifecycle retention, encryption, and repository access policy. Images are promoted by immutable
release version or commit SHA.
