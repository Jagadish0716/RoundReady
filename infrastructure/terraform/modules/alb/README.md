# ALB module

Planned interface: HTTPS listener, ACM certificate, HTTP redirect, security groups, health
checks, and internal target routing. The ALB is the only public application ingress; Kubernetes
services remain internal unless explicitly approved.
