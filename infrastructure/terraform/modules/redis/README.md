# ElastiCache Redis module

Planned interface: private subnet group, authenticated TLS endpoint, encryption in transit/at
rest, parameter group, security groups, and conservative node sizing. Redis is not publicly
accessible and is used for the application cache/locking needs already defined by the services.
